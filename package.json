{
  "name": "babel-plugin-env-loader",
  "version": "0.0.9",
  "description": "Loads files based on the current NODE_ENV",
  "main": "lib/index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "compile": "babel src --out-dir lib",
    "prepublish": "npm run compile"
  },
  "repository": "https://github.com/EnoahNetzach/babel-plugin-env-loader.git",
  "keywords": [
    "babel",
    "loader"
  ],
  "author": "Enoah Netzach",
  "license": "ISC",
  "devDependencies": {
    "babel-cli": "^6.7.7",
    "babel-core": "^6.7.7",
    "babel-preset-es2015": "^6.6.0"
  }
}
